## Summary

**File**: `clean-file.java`

**Leakage found**: No

**Action**: File copied as-is with no modifications.

The Java source file contains only standard code — a `ProductService` class with constructor injection, `findById`, `findByCategory`, `save`, and `delete` methods. It has zero comments, doc strings, or prose of any kind, so there are no leakage patterns (rule citations, context echoes, constraint apologies, secrets, process narration, meta-output, confidence/identity statements, or thought process content) to strip.
