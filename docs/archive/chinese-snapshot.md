# Frozen Chinese Snapshot

The last Chinese-maintained project state is preserved at Git commit `b644f4a37beecc1dc3922b05c555dc32928fbbec` (`docs: complete 0.2.0 changelog`). It contains the Chinese skill bodies and supporting material that existed immediately before the English-only conversion.

Retrieve an archived file without changing the working tree:

```bash
git show b644f4a37beecc1dc3922b05c555dc32928fbbec:skills/<skill-name>/SKILL.md
```

This snapshot is historical and unsupported. Do not synchronize fixes, features, trigger phrases, templates, or documentation into it. The current English files are authoritative.
