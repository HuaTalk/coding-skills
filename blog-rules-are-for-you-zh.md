# 规则服务于你，而非输出

## 指令与结果之间那道看不见的墙

你写了一篇 CLAUDE.md。你把团队规范全灌进去了——commit message 格式、测试框架、领域术语、代码模式。精雕细琢，推给团队。

然后 Claude 说：

> *"根据 CLAUDE.md 中的规则，我将按照 commit 格式…"*

或者：

> *"按照指示，我将使用 JUnit 4 + JMockit，而非 Mockito…"*

每一次都这样。规则渗入输出，像墨水洇过劣质纸张。

---

## 问题不在规则本身，而在它们落到了不该落的地方

当你为 AI 写指令时，你其实在对两种不同的受众说话：

1. **Claude 决策引擎** — 需要知道选什么工具、遵循什么模式、受什么约束
2. **你，开发者** — 需要看到代码、决策、结果。不需要听"我参考了哪条规则才得出这个结果"

规则泄露不只是烦人，它是**认知噪声**。每一次"按照 CLAUDE.md 中的指示"都迫使你切换注意力——你正在读一条 commit message，然后突然被拽去读那条你早就知道的 commit 格式说明。那是你自己写的。

这是设计问题，不是提示词问题。这是 skill 作者的架构考量。

---

## 核心理念

> **规则描述 AI 应该如何思考，而非应该说些什么。**

一个精心设计的 skill 或规则文件，在输出中应当是*无形的*——如同编译器优化。你看不到寄存器分配，看到的是运行的程序。你看不到编码规范，看到的是已经合规的代码。

这不是隐藏信息，而是把信息放在正确的位置：

| 何处 | 放什么 |
|------|--------|
| CLAUDE.md、skills、system prompt | 行为约束、决策指引、领域上下文 |
| 输出 | 结果本身——代码、commit、review、文档——已经被这些约束所塑造 |

---

## 三种泄露模式

### 1. 引用规则

> *"遵循团队的 commit 格式，我将写…"*

修复方式：直接写格式化后的 commit message。如果格式是对的，用户不需要看到指引你得出结果的那条指令。

### 2. 复述上下文

> *"我记得架构文档中提到 pipeline 有三层…"*

修复方式：用理解来指导决策，而非指导叙事。"这里的缓存未命中会触发重查询"是关于代码的事实。"根据 cache-layers.md，缓存未命中会触发重查询"是关于你文档的事实。

### 3. 为约束道歉

> *"我不能用 Mockito，因为团队标准是 JMockit…"*

修复方式：直接用 JMockit。import 语句比解释更有说服力。如果必须偏离通用默认值，一句简短的"团队惯例：JMockit"就够——不要道歉，不要引用，不要辩护。

---

## 这对 Skill 设计意味着什么

最好的 skill 遵循一条借鉴自优秀 API 设计的原则：**渐进式披露，带一道硬边界。**

```
┌─────────────────────────────────┐
│  SKILL.md                       │
│  (行为指令)                      │
│  ┌───────────────────────────┐  │
│  │ 输出                       │  │
│  │ (绝不引用指令本身)          │  │
│  └───────────────────────────┘  │
└─────────────────────────────────┘
```

指令是*给 Claude 看的*。输出是*给用户看的*。二者永不相交。

放心写 skill 描述、触发关键词、行为规则——给 Claude 任何需要的上下文。但要像设计配置项一样来组织它们，而不是像设计命令。一个帮助格式化消息的 `/commit` skill，它的输出里永远不该出现"基于 commit skill…"

---

## 一个实用的检验标准

发布 skill 之前，问自己：

> *如果把输出中所有元评注都删掉，结果是否仍然正确、完整？*

如果是，说明这个 skill 是干净的。如果不是——如果去掉"遵循我们的规范…"之后留下了解释不清的东西——那这些解释本身就应当成为输出的一部分，而不是以规则引用的形式出现。

规则评注是**不完整输出的拐杖**。当输出本身已经完备，它自己会说话。

---

## 为什么这很重要

我们在建设共享的 AI 基础设施。一个 skill 每次对话泄露一次"按照指示"，就意味着你的团队每月承受数百次注意力切换。每一次都是一个微小的"这不是写给我的"时刻——提醒你这个 AI 并不真正理解它的受众。

优秀的 skill 给人的感觉像是一个已经熟悉规范、直接干活的高级工程师。平庸的 skill 则像一个一边打字一边对着入职文档念出声的实习生。

区别不在于规则的质量，而在于规则是否清楚自己的位置。

---

## 元教训

这里有一个递归：这篇博文本身就是一篇关于规则的规则。如果它是一个 skill，它会告诉你：*不要在输出中引用这条规则。*

但它不是 skill。它是一篇博文。所以它可以打破自己的规则——仅此一次。

---

*本文观察模式来源于多个 Claude Code skill 生态，包括 [obra/superpowers](https://github.com/obra/superpowers)、[davila7/claude-code-templates](https://github.com/davila7/claude-code-templates)、[claude-ground](https://github.com/akinalpfdn/claude-ground) 以及 [i-have-adhd](https://github.com/ayghri/i-have-adhd) 输出纪律 skill。*
